[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["pip install -r requirements.txt --break-system-packages"]

[start]
cmd = "gunicorn --bind 0.0.0.0:$PORT --timeout 120 --access-logfile - --error-logfile - --capture-output run:app"
